<resources>
    <!-- Compose用: AppThemeはComposeが上書きするのでシンプルに -->
    <style name="Theme.HairGrowthTracker" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>
